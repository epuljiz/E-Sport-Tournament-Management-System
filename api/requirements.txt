# =============================================================
# E-sports Tournament API — Python zavisnosti
# =============================================================

# -- Web framework --
fastapi[standard]>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0

# -- Validacija i konfiguracija --
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0

# -- Baza podataka --
sqlalchemy>=2.0,<3.0
asyncpg>=0.30,<1.0

# -- Migracije --
alembic>=1.14,<2.0

# -- Autentikacija --
passlib[bcrypt]>=1.7,<2.0
python-jose[cryptography]>=3.3,<4.0

# -- Testiranje --
httpx>=0.28,<1.0
pytest>=8.3,<9.0
pytest-asyncio>=0.25,<1.0
